保留问题(分区权重一样时怎么排序)

实现了分区的ServiceImpl，学到了转流执行Enity转VO的快捷函数，convertToVO其中用了BeanUtils的copy函数，浅拷贝(明确了Java的对象浅拷贝--引用拷贝时就是指针思想，只是一些引用对象不可变保护了其浅拷贝的风险)

DTO和VO本次分区使用ai的，下次就会了

改造到Controller层需要全局异常捕获@Valid，想检验一下@Valid返回400还是500	--其下的@Max等检查疑似无效

半升级异常半升级DTO和@Valid的Controller

@Valid之类的注解检查只能用于需要抛异常的，不想抛异常的不要用注解检查

10.20 10:11--测试分区功能，增删改查

测试hasRole--成功ADMIN

正在重写异常，异常没有捕获@Valid的，疑问Result统一返回和ErrorResponse会不会重复？好多POJO都有code

可以自动化测试一下，构造api 以apifox

10.21 11:08 -- 新建CategoryAdminVO，发现BeanUnitl的copy不会自动转换类型，即boolean不能变成Boolean，所以要保持转换前后类型一致