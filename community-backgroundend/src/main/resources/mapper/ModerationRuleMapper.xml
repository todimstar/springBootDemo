<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.liu.springbootdemo.mapper.ModerationRuleMapper">

    <!-- 分页查询规则 -->
    <select id="pageQuery" resultType="com.liu.springbootdemo.POJO.vo.ModerationRuleVO">
        SELECT id, rule_type, pattern, weight_score, enabled, description, created_at, updated_at
        FROM moderation_rule
        WHERE deleted = 0
        <if test="ruleType != null and ruleType != ''">
            AND rule_type = #{ruleType}
        </if>
        <if test="enabled != null">
            AND enabled = #{enabled}
        </if>
        ORDER BY created_at DESC
    </select>

    <!-- 动态更新规则 -->
    <update id="update">
        UPDATE moderation_rule
        <set>
            <if test="ruleType != null">rule_type = #{ruleType},</if>
            <if test="pattern != null">pattern = #{pattern},</if>
            <if test="weightScore != null">weight_score = #{weightScore},</if>
            <if test="enabled != null">enabled = #{enabled},</if>
            <if test="description != null">description = #{description},</if>
        </set>
        WHERE id = #{id} AND deleted = 0
    </update>

</mapper>
