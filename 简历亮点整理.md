# Spring Boot 社区论坛系统 - 简历亮点整理

> **项目定位**: 基于 Spring Boot 3.3.5 的社区论坛系统，采用前后端分离架构
> **技术栈**: Spring Boot 3 + Spring Security 6 + MyBatis + Redis + MinIO + MySQL
> **开发周期**: 2025.9 - 2026.1（持续迭代中）

---

## 🎯 核心技术亮点（按重要程度排序）

### 1. ⭐⭐⭐ Spring Security 6 + JWT 完整认证授权体系

**实现内容**:
- 自定义 `JwtAuthenticationFilter` 实现 Token 认证过滤
- 集成 `UserDetailsService` 实现用户认证和权限管理
- 配置白名单策略（登录、注册、Swagger、公开GET接口）
- 自定义异常处理（`AuthenticationEntryPoint`, `AccessDeniedHandler`）
- 支持基于角色的权限控制（`@PreAuthorize`）

**技术亮点**:
```
✓ 使用 Spring Boot 3.x 新版 SecurityFilterChain 配置（非旧版 WebSecurityConfigurerAdapter）
✓ JWT 工具类封装（生成、验证、解析）
✓ 使用 @ConfigurationProperties 从配置文件读取密钥和过期时间
✓ 无状态 Session 管理（STATELESS）
```

**简历描述**:
> "实现了基于 JWT 的用户认证授权系统，采用 Spring Security 6 + Filter Chain 架构，支持白名单配置和细粒度权限控制，保障系统安全性。"

---

### 2. ⭐⭐⭐ MinIO 分布式对象存储 + 智能图片压缩

**实现内容**:
- 集成 MinIO 对象存储服务，实现文件上传、下载、删除
- 文件分类管理（FileType 枚举：头像/封面/帖子图片/评论图片）
- UUID + 日期分层命名策略（避免冲突和路径穿越攻击）
- 用户更换头像时自动删除旧文件（资源管理优化）
- Bucket 公开读权限配置（减轻后端压力）

**图片压缩优化**:
```
✓ 使用 Thumbnailator 库实现智能压缩
✓ 按文件类型配置压缩策略（头像 512px/80%，封面 1280px/82%）
✓ 智能判断：GIF 不压缩、已足够小不压缩、压缩后更大保留原图
✓ 平均减少 60%-70% 文件大小，节省存储成本
```

**技术亮点**:
```
✓ @PostConstruct 初始化 Bucket 并设置权限
✓ FileType 枚举封装业务规则（文件夹、大小限制、描述）
✓ 自定义 InMemoryMultipartFile 实现压缩后文件对象
✓ record 类型（Java 14+）优雅管理压缩规则
```

**简历描述**:
> "负责文件存储模块，集成 MinIO 对象存储，实现文件分类管理和智能压缩，上传图片自动压缩 60%-70%，并通过 UUID + 日期分层命名和旧文件自动清理机制，保障系统安全性和存储效率。"

---

### 3. ⭐⭐⭐ 统一异常处理 + 标准化响应体系

**架构设计**:
- `BusinessException` 业务异常基类（封装 ErrorCode、HttpStatus、额外数据）
- `ErrorCode` 枚举管理所有错误码（40+ 种错误场景）
- `GlobalExceptionHandler` 全局异常拦截（@RestControllerAdvice）
- `Result<T>` 统一响应格式（包含 code、message、data）
- `ErrorResponse` 详细错误信息（含 path、requestId、timestamp）

**处理的异常类型**:
```
✓ 业务异常（BusinessException）
✓ 参数校验异常（@Valid、BindException、ConstraintViolationException）
✓ Spring Security 异常（AccessDenied、Authentication）
✓ HTTP 异常（404/405/415）
✓ 文件上传大小超限
✓ 兜底 Exception（生成 requestId 便于追踪）
```

**简历描述**:
> "设计并实现统一异常处理体系，通过自定义业务异常类 + ErrorCode 枚举 + 全局异常拦截器，覆盖 40+ 种异常场景，所有异常响应包含 requestId 便于问题追踪，提升系统可维护性。"

---

### 4. ⭐⭐ MapStruct 高性能对象映射

**实现内容**:
- 使用 MapStruct 替代 BeanUtils（性能提升 10 倍以上）
- 编译期生成映射代码（非反射）
- 支持多源映射（Post + User → PostSummaryVO）
- 自动处理同名属性，手动配置冲突字段

**技术亮点**:
```
✓ @Mapper(componentModel = "spring") 支持 Spring 依赖注入
✓ @Mapping 注解处理字段冲突和不同名映射
✓ 编译期生成代码，零性能损耗
```

**简历描述**:
> "采用 MapStruct 对象映射框架替代传统 BeanUtils，通过编译期代码生成实现零反射损耗，性能提升 10 倍以上，并支持多源实体组合映射。"

---

### 5. ⭐⭐ AOP 切面编程 - 慢查询监控

**实现内容**:
- 使用 `@Aspect` + `@Around` 环绕通知实现性能监控
- 切点覆盖所有 Controller 方法
- 超过 500ms 自动记录慢查询警告
- 记录 URI、方法签名、耗时

**简历描述**:
> "通过 AOP 切面编程实现 Controller 层性能监控，自动记录慢查询（>500ms），无侵入式监控接口响应时间，便于性能优化。"

---

### 6. ⭐⭐ VO/DTO/Entity 三层分离架构

**设计模式**:
- **Entity**: 数据库实体（含所有字段，含敏感信息）
- **DTO**: 数据传输对象（接收客户端请求）
- **VO**: 视图对象（返回客户端，隐藏敏感信息）

**实践案例**:
```
✓ PostDetailVO: 帖子详情（含 content，无 summary）
✓ PostSummaryVO: 帖子摘要（含 summary + 作者信息，无 content）
✓ UpdateUserVO: 更新用户返回（隐藏密码）
```

**简历描述**:
> "采用 VO/DTO/Entity 三层分离架构，灵活控制前后端数据传输，避免敏感信息泄露和过度传输，提升系统安全性和性能。"

---

### 7. ⭐ Redis 缓存 + 序列化配置

**实现内容**:
- 集成 Redis 实现邮箱验证码缓存
- 自定义序列化配置（StringRedisSerializer + GenericJackson2JsonRedisSerializer）
- 防止中文乱码问题

**简历描述**:
> "集成 Redis 缓存实现验证码存储，自定义序列化配置防止乱码，支持数据过期自动清理。"

---

### 8. ⭐ Knife4j + SpringDoc 接口文档（版本兼容实践）

**实现内容**:
- 使用 Knife4j 4.5.0 + SpringDoc 2.7.0（手动升级）
- 解决 Spring Boot 3.5.x（Spring 6.2.x）版本兼容问题
- 完整的接口文档注解（@Schema、@Operation、@Parameter）

**技术难点**:
```
✓ Spring Boot 3.5.x 对应 Spring 6.2.x，需要 SpringDoc 2.7.0+
✓ Knife4j 4.5.0 内置 SpringDoc 版本太低，需手动升级
```

**简历描述**:
> "集成 Knife4j 接口文档，解决 Spring Boot 3.5.x 版本兼容问题（手动升级 SpringDoc 至 2.7.0），提供完整的 API 文档。"

---

### 9. ⭐ 枚举类的企业级应用

**设计亮点**:
- `FileType` 枚举封装文件类型业务规则（文件夹、大小限制、描述）
- `ErrorCode` 枚举管理所有错误码（避免魔法值）
- `PostStatus` 枚举管理帖子状态机（草稿 → 待审核 → 已发布）
- 枚举方法：`fromFolder()` 反向查找

**简历描述**:
> "大量使用枚举封装业务规则，如 FileType 枚举管理文件类型、大小限制和压缩策略，ErrorCode 枚举标准化错误码，提升代码可维护性。"

---

### 10. ⭐ MyBatis 动态 SQL + 分页插件

**实现内容**:
- MyBatis 动态 SQL（`<where>`, `<if>`, `<choose>`）
- LEFT JOIN 多表联查（Post + Category + User）
- PageHelper 分页插件（适配 Spring Boot 3）
- 配合 Spring Data Pageable 统一分页参数

---

## 📊 可量化成果

| 指标 | 数值 | 说明 |
|------|------|------|
| 接口数量 | 30+ | 涵盖用户、帖子、分区、评论、文件上传 |
| 图片压缩率 | 60%-70% | 平均减少文件大小，节省存储成本 |
| 对象映射性能提升 | 10倍+ | MapStruct vs BeanUtils |
| 异常场景覆盖 | 40+ 种 | 完整的异常处理体系 |
| 慢查询阈值 | 500ms | AOP 自动监控 |
| 数据库表 | 8张+ | User、Post、Category、Comment 等 |

---

## 🛠️ 完整技术栈

### 后端框架
- Spring Boot 3.3.5
- Spring Security 6.x
- Spring AOP
- Spring Data (Pageable)

### 持久层
- MyBatis 3.0.3
- PageHelper 2.1.0（分页）
- MySQL 8.0

### 缓存
- Redis (Spring Data Redis)

### 对象存储
- MinIO 8.5.7

### 工具库
- MapStruct 1.5.5（对象映射）
- Thumbnailator 0.4.20（图片压缩）
- JWT (jjwt 0.12.6)
- Lombok（减少样板代码）
- Validation（参数校验）

### 接口文档
- Knife4j 4.5.0 + SpringDoc 2.7.0

### 其他
- Jackson（JSON 序列化）
- JavaMail（邮箱验证码）

---

## 💡 项目难点与解决方案

### 1. Spring Boot 3.x 版本兼容问题
**问题**: Spring Boot 3.5.x 对应 Spring 6.2.x，Knife4j 内置 SpringDoc 版本跟不上
**解决**: 手动升级 SpringDoc 至 2.7.0，并验证所有依赖兼容性

### 2. 图片存储与性能优化
**问题**: 图片文件过大，消耗存储和带宽
**解决**: 
- 集成 Thumbnailator 智能压缩
- 按文件类型配置不同压缩策略
- MinIO Bucket 公开读，减轻后端压力

### 3. 用户头像更新资源浪费
**问题**: 用户频繁更换头像导致旧文件堆积
**解决**: 更换头像时自动删除旧文件（通过 URL 解析 objectName）

### 4. 异常信息混乱，难以追踪
**问题**: 异常响应格式不统一，难以定位问题
**解决**: 
- 统一异常处理体系（BusinessException + ErrorCode）
- 生成 requestId 便于日志追踪
- 开发环境返回详细错误信息

### 5. 参数校验重复代码多
**问题**: Controller 层大量参数校验代码
**解决**: 
- 使用 @Valid 注解 + DTO 校验
- 统一处理 MethodArgumentNotValidException
- 自定义校验注解

---

## 📝 简历项目描述模板

### 版本 1：详细版（200字）
> **Spring Boot 社区论坛系统** | 2025.9 - 2026.1
> 
> 基于 Spring Boot 3.3.5 + Spring Security 6 + MyBatis + Redis + MinIO 的前后端分离社区论坛系统。
> 
> **核心职责**:
> 1. 实现基于 JWT 的认证授权体系，采用 Spring Security 6 Filter Chain 架构，支持白名单和细粒度权限控制
> 2. 集成 MinIO 对象存储，实现文件分类管理和智能图片压缩（压缩率 60%-70%），自动清理过期文件
> 3. 设计统一异常处理体系，覆盖 40+ 种异常场景，所有响应包含 requestId 便于追踪
> 4. 采用 MapStruct 对象映射替代 BeanUtils，性能提升 10 倍，并实现 VO/DTO/Entity 三层分离
> 5. 通过 AOP 切面实现慢查询监控（>500ms），无侵入式性能监控
> 
> **技术栈**: Spring Boot 3、Spring Security 6、JWT、MyBatis、Redis、MinIO、MapStruct、Knife4j

### 版本 2：简洁版（100字）
> **Spring Boot 社区论坛系统** | 2025.9 - 2026.1
> 
> 基于 Spring Boot 3 + Spring Security 6 的社区论坛系统，实现 JWT 认证、MinIO 对象存储（图片智能压缩 60%-70%）、统一异常处理（40+ 场景）、MapStruct 高性能映射、AOP 慢查询监控等功能。采用 VO/DTO/Entity 三层架构，保障系统安全性和可维护性。
> 
> **技术栈**: Spring Boot 3、Spring Security、JWT、MyBatis、Redis、MinIO

---

## 🎤 面试问题准备

### 1. 为什么选择 MinIO 而不是阿里云 OSS？
**回答**: 
- 学习成本低，本地可部署，便于开发调试
- 开源免费，适合个人项目
- S3 兼容协议，迁移到云服务商成本低
- 实际生产环境可以无缝切换到阿里云 OSS/腾讯云 COS

### 2. 图片压缩策略是如何设计的？
**回答**:
- 使用 FileType 枚举封装不同类型文件的压缩规则
- 头像要求小，压缩到 512px/80%
- 帖子图片要求清晰，压缩到 1600px/85%
- GIF 不压缩（保持动画）
- 压缩后更大则保留原图（避免画质损失）
- 使用 record 类型优雅管理规则（Java 14+）

### 3. JWT 是如何实现的？
**回答**:
- JwtUtil 工具类封装生成、验证、解析逻辑
- JwtAuthenticationFilter 过滤器拦截请求并验证 Token
- 从 Token 提取用户名，加载 UserDetails 存入 SecurityContext
- 白名单配置，登录、注册、公开接口无需验证
- 异常处理：未认证返回 401，权限不足返回 403

### 4. 统一异常处理是如何实现的？
**回答**:
- BusinessException 封装业务异常（ErrorCode + HttpStatus）
- GlobalExceptionHandler 使用 @RestControllerAdvice 全局拦截
- ErrorCode 枚举标准化错误码（40+ 种）
- 生成 requestId 便于日志追踪
- 开发环境返回详细错误，生产环境返回简洁提示

### 5. MapStruct 比 BeanUtils 好在哪里？
**回答**:
- **性能**: 编译期生成代码，无反射，性能提升 10 倍以上
- **安全**: 编译期检查，类型不匹配直接报错
- **灵活**: 支持多源映射、自定义转换、字段重命名
- **可维护**: 接口定义清晰，代码可读性高

### 6. 如何保证头像更换时旧文件被删除？
**回答**:
- User 表存储 avatarUrl（完整 URL）
- 更新头像时，先从 URL 解析出 objectName
- 调用 MinIO deleteFile() 删除旧文件
- 上传新文件后更新数据库 avatarUrl
- 使用事务保证数据一致性

### 7. 慢查询监控是如何实现的？
**回答**:
- 使用 @Aspect + @Around 环绕通知
- 切点: execution(* controller..*.*(..))
- 记录方法执行开始时间和结束时间
- 超过 500ms 记录 WARN 日志（URI、方法、耗时）
- 低于 500ms 记录 INFO 日志

---

## 📚 推荐复习资料

### 核心技术点
1. **Spring Security 6**: 官方文档 + 实战案例
2. **JWT**: RFC 7519 标准 + jjwt 库文档
3. **MapStruct**: 官方文档 + 最佳实践
4. **MinIO**: 官方文档 + S3 协议
5. **AOP**: Spring AOP 官方文档

### 面试高频问题
1. Spring Security 认证流程
2. JWT vs Session 优缺点
3. AOP 实现原理（动态代理）
4. Redis 缓存策略（缓存穿透/击穿/雪崩）
5. 分布式文件存储方案对比

---

**最后更新**: 2026-01-27
**状态**: 持续迭代中，准备找实习 🚀
