# TechForum 技术选型文档

## 🏗️ 技术架构

### 整体架构
```
┌─────────────┐
│   前端层    │  Vue3 + Element Plus
├─────────────┤
│   网关层    │  Nginx（可选）
├─────────────┤
│   应用层    │  Spring Boot 3.x
├─────────────┤
│   缓存层    │  Redis 7.x
├─────────────┤
│   存储层    │  MySQL 8.0 + MinIO
└─────────────┘
```

---

## 💻 后端技术栈

### 1. 核心框架

| 技术 | 版本 | 用途 | 状态 |
|------|------|------|------|
| Spring Boot | 3.2.x | 基础框架 | ✅ 已用 |
| Spring Security | 6.x | 安全认证 | ✅ 已用 |
| MyBatis | 3.5.x | ORM框架 | ✅ 已用 |
| JWT | 0.12.x | Token生成 | ✅ 已用 |

**选型理由**：
- Spring Boot：快速开发，生态完善，企业主流选择
- Spring Security：强大的安全框架，支持多种认证方式
- MyBatis：灵活的SQL控制，适合复杂查询场景
- JWT：无状态认证，支持分布式部署

---

### 2. 数据存储

| 技术 | 版本 | 用途 | 优先级 |
|------|------|------|--------|
| MySQL | 8.0+ | 主数据库 | 🔥 必须 |
| Redis | 7.x | 缓存/计数 | 🔥 必须 |
| MinIO | latest | 图片存储 | 🔥 必须 |
| Elasticsearch | 8.x | 全文搜索 | ⚠️ 可选 |

#### MySQL 8.0
**选型理由**：
- 性能优化：支持窗口函数、JSON函数
- 字符集完善：utf8mb4支持emoji
- 索引优化：InnoDB存储引擎性能好

**配置建议**：
```yaml
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/tech_forum?useUnicode=true&characterEncoding=utf8mb4&serverTimezone=Asia/Shanghai
    username: root
    password: your_password
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      connection-timeout: 30000
```

#### Redis 7.x
**应用场景**：
1. **热点数据缓存**
   - 热门帖子列表（缓存1小时）
   - 分区列表（缓存24小时）
   - 用户信息（缓存30分钟）

2. **计数器**
   - 帖子浏览量（实时累加）
   - 点赞数（实时累加）
   - 评论数（实时累加）

3. **接口限流**
   - 每分钟请求次数限制
   - 防止恶意刷接口

4. **分布式锁**（可选）
   - 防止重复点赞
   - 并发控制

**配置示例**：
```yaml
spring:
  data:
    redis:
      host: localhost
      port: 6379
      password: 
      database: 0
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 2
```

**使用示例**：
```java
// 1. 缓存帖子详情
@Cacheable(value = "posts", key = "#postId", unless = "#result == null")
public Post getPostById(Long postId) {
    return postMapper.findById(postId);
}

// 2. 浏览量计数
public void incrementViewCount(Long postId) {
    redisTemplate.opsForValue().increment("post:view:" + postId);
}

// 3. 接口限流
public boolean checkRateLimit(String ip) {
    String key = "rate:limit:" + ip;
    Long count = redisTemplate.opsForValue().increment(key);
    if (count == 1) {
        redisTemplate.expire(key, 60, TimeUnit.SECONDS);
    }
    return count <= 100; // 每分钟100次
}
```

#### MinIO
**选型理由**：
- 开源免费，本地部署
- S3兼容，易于迁移到云存储
- 学习成本低，部署简单

**Docker部署**：
```bash
docker run -d \
  -p 9000:9000 \
  -p 9001:9001 \
  --name minio \
  -e "MINIO_ROOT_USER=admin" \
  -e "MINIO_ROOT_PASSWORD=admin123456" \
  -v /data/minio:/data \
  minio/minio server /data --console-address ":9001"
```

**使用示例**：
```java
@Service
public class FileService {
    @Autowired
    private MinioClient minioClient;
    
    public String uploadImage(MultipartFile file) throws Exception {
        // 1. 生成唯一文件名
        String fileName = UUID.randomUUID() + getExtension(file);
        
        // 2. 上传到MinIO
        minioClient.putObject(
            PutObjectArgs.builder()
                .bucket("forum-images")
                .object(fileName)
                .stream(file.getInputStream(), file.getSize(), -1)
                .contentType(file.getContentType())
                .build()
        );
        
        // 3. 返回访问URL
        return "http://localhost:9000/forum-images/" + fileName;
    }
}
```

#### Elasticsearch（可选，后期扩展）
**应用场景**：
- 全文搜索（支持中文分词）
- 搜索高亮
- 相关性排序

**不使用的理由**：
- 学习成本高
- 需要额外服务器资源
- 前期用MySQL LIKE足够

**何时引入**：
- 帖子数量超过10万
- 搜索性能成为瓶颈
- 需要复杂的搜索功能

---

### 3. 工具类库

| 技术 | 版本 | 用途 | 优先级 |
|------|------|------|--------|
| Lombok | latest | 简化代码 | ✅ 已用 |
| Validation | - | 参数校验 | 🔥 必须 |
| Jackson | - | JSON处理 | ✅ 已用 |
| Knife4j | 4.x | API文档 | 🔥 必须 |
| Hutool | 5.x | 工具类 | ⚠️ 推荐 |

#### Knife4j（Swagger增强版）
**添加依赖**：
```xml
<dependency>
    <groupId>com.github.xiaoymin</groupId>
    <artifactId>knife4j-openapi3-jakarta-spring-boot-starter</artifactId>
    <version>4.3.0</version>
</dependency>
```

**配置示例**：
```java
@Configuration
public class Knife4jConfig {
    @Bean
    public OpenAPI customOpenAPI() {
        return new OpenAPI()
            .info(new Info()
                .title("TechForum API文档")
                .version("1.0")
                .description("社区论坛接口文档"));
    }
}
```

**访问地址**：http://localhost:8080/doc.html

#### Hutool工具类
**常用功能**：
```java
// 1. 字符串工具
StrUtil.isBlank(str);
StrUtil.format("用户{}登录", username);

// 2. 日期工具
DateUtil.now();
DateUtil.format(date, "yyyy-MM-dd HH:mm:ss");

// 3. 加密工具
SecureUtil.md5(password);

// 4. HTTP工具
HttpUtil.get("https://api.example.com");
```

---

### 4. 消息队列（可选）

| 技术 | 版本 | 用途 | 优先级 |
|------|------|------|--------|
| RabbitMQ | 3.x | 异步任务 | ⚠️ 可选 |
| Kafka | 3.x | 日志收集 | ⚠️ 可选 |

**应用场景**：
- 发送邮件通知（异步）
- 点赞数同步到数据库（削峰填谷）
- 浏览量批量更新

**何时引入**：
- 系统并发量大
- 需要解耦业务逻辑
- 需要保证消息可靠性

**前期替代方案**：
- 使用`@Async`异步方法
- 定时任务批量处理

---

## 🎨 前端技术栈

### 核心框架

| 技术 | 版本 | 用途 |
|------|------|------|
| Vue 3 | 3.x | 前端框架 |
| Vue Router | 4.x | 路由管理 |
| Pinia | 2.x | 状态管理 |
| Axios | 1.x | HTTP请求 |
| Element Plus | 2.x | UI组件库 |
| Vite | 5.x | 构建工具 |

### 功能组件

| 组件 | 用途 |
|------|------|
| mavon-editor | Markdown编辑器 |
| highlight.js | 代码高亮 |
| vditor | Markdown渲染 |
| moment.js | 时间处理 |
| lodash | 工具函数 |

---

## 🔧 开发工具

### IDE & 编辑器
- **IntelliJ IDEA Ultimate** - Java开发（推荐）
- **VS Code** - 前端开发
- **Navicat/DataGrip** - 数据库管理

### 版本控制
- **Git** - 版本控制
- **GitHub/Gitee** - 代码托管

### 接口测试
- **Apifox** - 接口测试（推荐，国产）
- **Postman** - 接口测试
- **Knife4j** - API文档 + 在线测试

### 原型设计
- **Figma** - 原型设计（推荐）
- **墨刀** - 原型设计
- **draw.io** - 流程图

### 数据库设计
- **dbdiagram.io** - 在线ER图设计
- **PDMan** - 数据库建模工具

---

## 🚀 部署方案

### 开发环境
```
本地运行：
- MySQL: localhost:3306
- Redis: localhost:6379
- MinIO: localhost:9000
- 后端: localhost:8080
- 前端: localhost:5173
```

### 生产环境（Docker Compose）
```yaml
version: '3.8'
services:
  mysql:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: your_password
      MYSQL_DATABASE: tech_forum
    volumes:
      - mysql_data:/var/lib/mysql
    ports:
      - "3306:3306"
  
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
  
  minio:
    image: minio/minio
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: admin
      MINIO_ROOT_PASSWORD: admin123456
    volumes:
      - minio_data:/data
    ports:
      - "9000:9000"
      - "9001:9001"
  
  app:
    build: .
    depends_on:
      - mysql
      - redis
      - minio
    ports:
      - "8080:8080"
    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://mysql:3306/tech_forum
      SPRING_REDIS_HOST: redis
      MINIO_ENDPOINT: http://minio:9000

volumes:
  mysql_data:
  minio_data:
```

---

## 📦 Maven依赖清单

### 完整的pom.xml关键依赖

```xml
<dependencies>
    <!-- Spring Boot Starter -->
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    
    <!-- Spring Security -->
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-security</artifactId>
    </dependency>
    
    <!-- MyBatis -->
    <dependency>
        <groupId>org.mybatis.spring.boot</groupId>
        <artifactId>mybatis-spring-boot-starter</artifactId>
        <version>3.0.3</version>
    </dependency>
    
    <!-- MySQL Driver -->
    <dependency>
        <groupId>com.mysql</groupId>
        <artifactId>mysql-connector-j</artifactId>
    </dependency>
    
    <!-- Redis -->
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-data-redis</artifactId>
    </dependency>
    
    <!-- JWT -->
    <dependency>
        <groupId>io.jsonwebtoken</groupId>
        <artifactId>jjwt-api</artifactId>
        <version>0.12.3</version>
    </dependency>
    <dependency>
        <groupId>io.jsonwebtoken</groupId>
        <artifactId>jjwt-impl</artifactId>
        <version>0.12.3</version>
    </dependency>
    <dependency>
        <groupId>io.jsonwebtoken</groupId>
        <artifactId>jjwt-jackson</artifactId>
        <version>0.12.3</version>
    </dependency>
    
    <!-- MinIO -->
    <dependency>
        <groupId>io.minio</groupId>
        <artifactId>minio</artifactId>
        <version>8.5.7</version>
    </dependency>
    
    <!-- Validation -->
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-validation</artifactId>
    </dependency>
    
    <!-- Lombok -->
    <dependency>
        <groupId>org.projectlombok</groupId>
        <artifactId>lombok</artifactId>
        <optional>true</optional>
    </dependency>
    
    <!-- Knife4j -->
    <dependency>
        <groupId>com.github.xiaoymin</groupId>
        <artifactId>knife4j-openapi3-jakarta-spring-boot-starter</artifactId>
        <version>4.3.0</version>
    </dependency>
    
    <!-- Hutool -->
    <dependency>
        <groupId>cn.hutool</groupId>
        <artifactId>hutool-all</artifactId>
        <version>5.8.24</version>
    </dependency>
    
    <!-- Test -->
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-test</artifactId>
        <scope>test</scope>
    </dependency>
</dependencies>
```

---

## ⚡ 性能优化方案

### 1. 数据库优化
- **索引优化**：为常用查询字段添加索引
- **分页优化**：使用游标分页避免深分页
- **读写分离**：主从复制（后期）
- **分库分表**：数据量大时考虑（后期）

### 2. 缓存策略
- **多级缓存**：Redis + 本地缓存（Caffeine）
- **缓存预热**：系统启动时加载热点数据
- **缓存穿透**：布隆过滤器
- **缓存雪崩**：设置随机过期时间

### 3. 接口优化
- **接口限流**：令牌桶算法
- **异步处理**：@Async注解
- **批量操作**：减少数据库交互次数

---

## 🔒 安全方案

### 1. 认证授权
- JWT Token认证
- RBAC角色权限控制
- 密码BCrypt加密

### 2. 接口安全
- 参数校验（@Valid）
- SQL注入防护（MyBatis预编译）
- XSS防护（前端转义）
- CSRF防护（Token机制）

### 3. 文件上传安全
- 文件类型校验
- 文件大小限制
- 文件名重命名
- 病毒扫描（可选）

---

## 📊 监控方案（后期）

### 应用监控
- **Spring Boot Actuator** - 应用健康检查
- **Prometheus + Grafana** - 指标监控
- **ELK** - 日志收集分析

### 性能监控
- **JVM监控** - 内存、GC
- **接口性能** - 响应时间、QPS
- **数据库监控** - 慢查询

---

## 🎯 技术选型总结

### 必须使用（P0）
✅ Spring Boot + MyBatis + MySQL  
✅ Spring Security + JWT  
✅ Redis缓存  
✅ MinIO图片存储  
✅ Validation参数校验  
✅ Knife4j接口文档

### 推荐使用（P1）
⭐ Hutool工具类  
⭐ Docker部署  
⭐ Git版本控制

### 可选使用（P2）
⚠️ Elasticsearch全文搜索  
⚠️ RabbitMQ消息队列  
⚠️ 监控系统

### 暂不使用
❌ 微服务架构（Spring Cloud）  
❌ 分库分表（ShardingSphere）  
❌ 配置中心（Nacos）

**选型原则**：
1. 优先使用稳定成熟的技术
2. 避免过度设计
3. 技术服务于业务
4. 循序渐进，逐步优化

---

**文档版本**: v1.0  
**最后更新**: 2025-01-15  
**负责人**: 项目负责人